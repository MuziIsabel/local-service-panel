# Local Service Panel - 开发环境配置
#
# 复制为 .env 并根据需要修改

# 开发 token：在开发环境中绕过 Agent API 鉴权
# 启动 Agent 后自动生成 token 在 .data/config/token 中
# 设置此环境变量后，UI 将使用此 token 连接 Agent
# VITE_DEV_TOKEN=dev-token-for-testing-only
